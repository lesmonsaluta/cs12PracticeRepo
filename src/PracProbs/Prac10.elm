module PracProbs.Prac10 exposing (..)

flatProduct: List (List Int) -> Int


main =
    let
        -- Change the text inside the parentheses with a call to your function
        _ = Debug.log "" (onlyGreaterThan3 [4,4,4,4,4])
    in
    -- Ignore the line below
    Html.text ""
    